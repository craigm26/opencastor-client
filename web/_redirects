# Cloudflare Pages SPA fallback — serve index.html for all unmatched paths.
# Required for PathUrlStrategy (clean URLs). Safe to have with HashUrlStrategy too.
# Must be last rule; CF Pages evaluates top-to-bottom.
/*    /index.html   200
